#!/bin/bash
echo "🚀 Starting R2D Reconciliation Tool..."
echo "================================================"

# Change to script directory
cd "$(dirname "$0")"

# Run the launcher
python3 launch_app.py
